# Yealink MCore Provisioning Repository

This repository provides auto-provisioning configurations for Yealink MCore devices deployed in conference rooms.

When configured, each MCore will automatically pull configuration files from this repository over HTTPS using a URL like:

